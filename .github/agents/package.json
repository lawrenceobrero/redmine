{
  "name": "redmine-review-agents",
  "version": "0.1.0",
  "private": true,
  "description": "Reviewer / Synthesis / Fixer agents for the PR review pipeline demo. Built fresh for this demo; no employer code or prompts.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts",
    "demo": "tsx src/cli.ts",
    "live": "tsx src/live.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.112.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
